Sharbat is a Guice-like dependency injection framework for PHP. Allconfigurations are done through modules, which means there are no xml, ini orother config files.For now see the example directory to learn how to use Sharbat.TODO:	improve constant binding	add support for recursive dependencies	write tests	write documentation